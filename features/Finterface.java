//FUNCTIONAL INTERFACE
    // it will have exactly one abstact method and in functional interface it does't have a body 
    
package features;

@FunctionalInterface
interface Finterface {
    String sound();
} 
